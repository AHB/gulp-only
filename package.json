{
  "name": "gulp-only",
  "version": "2.0.0",
  "description": "Simple gulp plugin to search for describe.only and it.only",
  "main": "./lib/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/AHB/gulp-only.git"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "./node_modules/.bin/babel src -d lib",
    "prepublish": "npm run build"
  },
  "author": "AHB",
  "license": "ISC",
  "devDependencies": {
    "gulp-util": "3.0.7",
    "through2": "2.0.1",
    "babel-cli": "6.5.1",
    "babel-preset-es2015": "6.5.0"
  }
}
